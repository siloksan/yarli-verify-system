# Web Client — QR Component Verification (MVP)

PWA-клиент для системы верификации компонентов по QR-кодам.  
Используется исполнителями на производстве для проверки корректности
компонентов и партий при сборке заказа.

Проект является частью MVP и предназначен для демонстрации заказчику
предотвращения человеческих ошибок.

---

## 🎯 Назначение клиента

Клиентское приложение позволяет:

- получать список производственных заказов с сервера
- открывать рецепт (заказ) и видеть список компонентов
- сканировать QR-коды партий компонентов
- мгновенно определять:
  - корректный компонент / партия
  - ошибку выбора
- фиксировать результат сканирования на сервере

Приложение **mobile-first**, ориентировано на работу с телефона.

---

## 🧱 Архитектура

Используется **feature-based архитектура** (по доменам), без жёсткой привязки к UI-фреймворкам.

Ключевые принципы:

- разделение:
  - server state (React Query)
  - client state (Zustand)
- изоляция бизнес-логики от UI
- единая HTTP-прослойка
- масштабируемость без переезда на другую архитектуру

---

## 🛠 Технологический стек

### Основное

- **React 18**
- **TypeScript**
- **Vite**
- **React Router**
- **@tanstack/react-query** — работа с серверными данными
- **Zustand** — локальное состояние (сканирование, UI, временные флаги)
- **PWA** (service worker, manifest)

### Вспомогательное

- Fetch API (через обёртку `http.ts`)
- ESLint + Prettier
- Mobile-first CSS

---

## 📁 Структура проекта

app/
├# Инициализация приложения
│── router.tsx # Роутинг
│── providers.tsx # Общие провайдеры
│
├── pages/ # Страницы (routes)
│ ├── orders/
│ │ └── OrdersPage.tsx
│ └── recipe/
│ └── RecipePage.tsx
│
├── features/ # Бизнес-фичи
│ ├── orders/
│ │ ├── api/
│ │ │ └── orders.api.ts
│ │ ├── hooks/
│ │ │ └── useOrders.ts
│ │ └── types.ts
│ │
│ ├── recipe/
│ │ ├── api/
│ │ ├── hooks/
│ │ ├── store/ # Zustand
│ │ └── types.ts
│ │
│ └── qr-scan/
│ ├── components/
│ ├── store/
│ └── utils/
│
├── shared/
│ ├── api/ # Общие API-хелперы
│ ├── lib/
│ │ └── http.ts # HTTP-обёртка
│ ├── ui/ # Переиспользуемые UI-компоненты
│ └── types/ # Общие типы
│
├── assets/
├── styles/
└── main.tsx
